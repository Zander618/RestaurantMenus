# Installation

FIRST
CD INTO SERVER:
"bundle install"

seed ?

TO START SERVER:
"bundle exec rake server"

SECOND
CD INTO FROM:
Use "npm install" to get the packages. Then "npm start" to start the application.